# $Header: /u/gcmpack/MITgcm/verification/aim.5l_cs/input/Attic/.genmakerc,v 1.4 2003/06/27 21:40:49 cnh Exp $
# $Name:  $

# Force use of PGI f77
#if (`uname` == 'Linux') set platform=( Linux+pgi )

# Disable packages that are not needed
set DISABLE=( $DISABLE kpp gmredi obcs mom_fluxform zonal_filt )

# Enable packages that are needed but are disabled by default
set ENABLE=( $ENABLE aim_v23 land )

# We can also specify other defaults
set ROOTDIR=../../..
set MODS=../code
